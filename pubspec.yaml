name: test_firebase_0304
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.6 <3.0.0'

dependencies:

  cupertino_icons: ^1.0.2

  flutter:
    sdk: flutter

  firebase_core: ^2.7.0
  firebase_auth: ^4.2.9
  flutter_riverpod: ^2.3.0
  cloud_firestore: ^4.4.3
  firebase_storage: ^11.0.14
  image_picker: ^0.8.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true
